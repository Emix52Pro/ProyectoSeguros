import { FiltrarSegurosPipe } from './filtrar-seguros.pipe';

describe('FiltrarSegurosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarSegurosPipe();
    expect(pipe).toBeTruthy();
  });
});
